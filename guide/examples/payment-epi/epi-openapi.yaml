# Generated by tools/epi-check/openapi.mjs from tools/epi-check/contract/dto.schema.json. Do not edit by hand.
openapi: "3.1.0"
info:
  title: "Payment EPI (the service you implement)"
  version: "1.0.0"
  description: "The ten routes that CommerceOS calls on a payment EPI (External Partner Interface), under the base URL of the payment integration record. Bare routes carry no context headers. Contextful routes carry the three X-EPI-Context headers. Generated from the conformance tool's contract; the contract reference explains every field."
servers:
  - url: "{baseUrl}"
    variables:
      baseUrl:
        default: "https://piggy.example.com/piggy"
        description: "The baseUrl of the payment integration record in CommerceOS"
paths:
  "/install":
    post:
      summary: "Install: receive the OAuth2 client for the calls back to CommerceOS"
      requestBody:
        required: true
        content:
          "application/json":
            schema:
              "$ref": "#/components/schemas/InstallPayload"
      responses:
        "2XX":
          description: "Accepted. The body is ignored."
        "4XX":
          description: "A failed call. The error body is the ErrorBody schema."
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/ErrorBody"
  "/uninstall":
    post:
      summary: "Uninstall"
      responses:
        "2XX":
          description: "Accepted. The body is ignored."
        "4XX":
          description: "A failed call. The error body is the ErrorBody schema."
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/ErrorBody"
  "/config-schema":
    get:
      summary: "The form that an administrator fills in per organization node"
      responses:
        "200":
          description: "A form description: { title?, description?, members: { <key>: { type, title?, description?, members? } } }. See the reference, section 4."
          content:
            "application/json":
              schema:
                type: "object"
                additionalProperties: true
        "4XX":
          description: "A failed call. The error body is the ErrorBody schema."
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/ErrorBody"
  "/test":
    post:
      summary: "Test the configuration of the context"
      parameters:
        - "$ref": "#/components/parameters/ConfigId"
        - "$ref": "#/components/parameters/ConfigHash"
        - "$ref": "#/components/parameters/DebugInfo"
      responses:
        "200":
          description: "The configuration works."
          content:
            "application/json":
              schema:
                type: "boolean"
                const: true
        "4XX":
          description: "A failed call. The error body is the ErrorBody schema."
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/ErrorBody"
  "/methods":
    get:
      summary: "The payment methods that this configuration offers"
      parameters:
        - "$ref": "#/components/parameters/ConfigId"
        - "$ref": "#/components/parameters/ConfigHash"
        - "$ref": "#/components/parameters/DebugInfo"
      responses:
        "200":
          description: "OK"
          content:
            "application/json":
              schema:
                type: "array"
                items:
                  "$ref": "#/components/schemas/MethodDto"
        "4XX":
          description: "A failed call. The error body is the ErrorBody schema."
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/ErrorBody"
  "/terminals":
    get:
      summary: "The terminals that this configuration knows"
      parameters:
        - "$ref": "#/components/parameters/ConfigId"
        - "$ref": "#/components/parameters/ConfigHash"
        - "$ref": "#/components/parameters/DebugInfo"
      responses:
        "200":
          description: "OK"
          content:
            "application/json":
              schema:
                type: "array"
                items:
                  "$ref": "#/components/schemas/TerminalDto"
        "4XX":
          description: "A failed call. The error body is the ErrorBody schema."
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/ErrorBody"
  "/terminals/{terminalId}":
    get:
      summary: "One terminal"
      parameters:
        - name: "terminalId"
          in: "path"
          required: true
          schema:
            type: "string"
        - "$ref": "#/components/parameters/ConfigId"
        - "$ref": "#/components/parameters/ConfigHash"
        - "$ref": "#/components/parameters/DebugInfo"
      responses:
        "200":
          description: "OK"
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/TerminalDto"
        "4XX":
          description: "A failed call. The error body is the ErrorBody schema."
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/ErrorBody"
  "/payments/{paymentKey}":
    put:
      summary: "Start a payment: a stream of steps"
      description: "The response is a Server-Sent-Events stream (one `event:` line, one `data:` line, a blank line per step). Step types: Create, Cancellable, Wait, ShowImage, VisitPage, RenderView (intermediate); Complete, Decline, Cancel, Fail (final). A stream holds zero or more intermediate steps and exactly one final step. The data of each step is the schema of the same name, prefixed with Step: StepWait, StepComplete, and so on, or PaymentStep for any of them."
      parameters:
        - name: "paymentKey"
          in: "path"
          required: true
          schema:
            type: "string"
        - "$ref": "#/components/parameters/ConfigId"
        - "$ref": "#/components/parameters/ConfigHash"
        - "$ref": "#/components/parameters/DebugInfo"
      requestBody:
        required: true
        content:
          "application/json":
            schema:
              "$ref": "#/components/schemas/PaymentInitDto"
      responses:
        "200":
          description: "A stream of steps. Step types: Create, Cancellable, Wait, ShowImage, VisitPage, RenderView (intermediate); Complete, Decline, Cancel, Fail (final)."
          content:
            "text/event-stream":
              schema:
                type: "string"
        "4XX":
          description: "A failed call. The error body is the ErrorBody schema."
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/ErrorBody"
  "/payments/{paymentKey}/transactions":
    post:
      summary: "Capture, release or refund"
      parameters:
        - name: "paymentKey"
          in: "path"
          required: true
          schema:
            type: "string"
        - "$ref": "#/components/parameters/ConfigId"
        - "$ref": "#/components/parameters/ConfigHash"
        - "$ref": "#/components/parameters/DebugInfo"
      requestBody:
        required: true
        content:
          "application/json":
            schema:
              "$ref": "#/components/schemas/TransactionInitDto"
      responses:
        "200":
          description: "OK"
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/TransactionDto"
        "4XX":
          description: "A failed call. The error body is the ErrorBody schema."
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/ErrorBody"
  "/payments/{cancellationToken}/cancel":
    post:
      summary: "Cancel a payment that sent a Cancellable step"
      parameters:
        - name: "cancellationToken"
          in: "path"
          required: true
          schema:
            type: "string"
        - "$ref": "#/components/parameters/ConfigId"
        - "$ref": "#/components/parameters/ConfigHash"
        - "$ref": "#/components/parameters/DebugInfo"
      requestBody:
        required: true
        content:
          "application/json":
            schema:
              "$ref": "#/components/schemas/CancelDto"
      responses:
        "2XX":
          description: "Accepted. The body is ignored."
        "4XX":
          description: "A failed call. The error body is the ErrorBody schema."
          content:
            "application/json":
              schema:
                "$ref": "#/components/schemas/ErrorBody"
components:
  parameters:
    ConfigId:
      name: "X-EPI-Context-Config-Id"
      in: "header"
      required: true
      schema:
        type: "string"
      description: "The four-character id of the EPI configuration"
    ConfigHash:
      name: "X-EPI-Context-Config-Hash"
      in: "header"
      required: true
      schema:
        type: "string"
      description: "A hash of the configuration values. A changed configuration has a new hash"
    DebugInfo:
      name: "X-EPI-Debug-Info"
      in: "header"
      required: true
      schema:
        type: "string"
      description: "JSON with nodeName, baseUrl and name. Logging only"
  schemas:
    DecimalDto:
      type: "string"
      pattern: "^-?\\d+(\\.\\d+)?$"
    PaymentDirection:
      type: "string"
      enum:
        - "Payment"
        - "Payout"
    PaymentDeclineReason:
      type: "string"
    Audience:
      type: "string"
      enum:
        - "merchant"
        - "customer"
        - "all"
    InstallPayload:
      type: "object"
      properties:
        cosBaseUrl:
          type: "string"
        tokenUrl:
          type: "string"
        clientId:
          type: "string"
        clientSecret:
          type: "string"
        scope:
          type: "string"
      required:
        - "cosBaseUrl"
        - "tokenUrl"
        - "clientId"
        - "clientSecret"
        - "scope"
    MethodDto:
      type: "object"
      properties:
        methodId:
          type: "string"
        name:
          type: "string"
        requires:
          type: "object"
          properties:
            terminal:
              type: "boolean"
            specification:
              type: "boolean"
          required:
            - "terminal"
            - "specification"
        supports:
          type: "object"
          properties:
            incoming:
              type: "boolean"
            outgoing:
              type: "boolean"
            reversal:
              type: "boolean"
          required:
            - "incoming"
            - "outgoing"
            - "reversal"
        allows:
          type: "object"
          properties:
            availableInPos:
              type: "boolean"
            openCashBoxOnUse:
              type: "boolean"
            openCashBoxOnReturn:
              type: "boolean"
            fullAmountFinishesReceipt:
              type: "boolean"
      required:
        - "methodId"
        - "name"
        - "requires"
        - "supports"
    TerminalDto:
      type: "object"
      properties:
        terminalId:
          type: "string"
        methodId:
          type: "string"
        name:
          type: "string"
        directUrl:
          type: "string"
      required:
        - "terminalId"
        - "methodId"
    LocalProxyDto:
      type: "object"
      properties:
        hostname:
          type: "string"
        secret:
          type: "string"
      required:
        - "hostname"
        - "secret"
    PaymentInitDto:
      type: "object"
      properties:
        methodId:
          type: "string"
        amount:
          "$ref": "#/components/schemas/DecimalDto"
        currencyCode:
          type: "string"
        direction:
          "$ref": "#/components/schemas/PaymentDirection"
        payer:
          "$ref": "#/components/schemas/AgentDto"
        payee:
          "$ref": "#/components/schemas/AgentDto"
        token:
          type: "string"
        locale:
          type: "string"
        specification:
          "$ref": "#/components/schemas/SpecificationDto"
        debitSynchronously:
          const: true
        redirectUrls:
          type: "object"
          properties:
            completed:
              type: "string"
            failed:
              type: "string"
            cancelled:
              type: "string"
          required:
            - "completed"
            - "failed"
            - "cancelled"
        termsUrl:
          type: "string"
        terminalId:
          type: "string"
        terminalDirectUrl:
          type: "string"
        localProxy:
          "$ref": "#/components/schemas/LocalProxyDto"
        isLocalTerminal:
          type: "boolean"
        walletCode:
          type: "string"
        walletPin:
          type: "string"
        cardAcquisitionReference:
          type: "object"
          properties:
            poiTransactionId:
              type: "string"
            timestamp:
              type: "string"
          required:
            - "poiTransactionId"
            - "timestamp"
      required:
        - "methodId"
        - "amount"
        - "currencyCode"
        - "direction"
        - "payer"
        - "payee"
        - "token"
        - "locale"
        - "specification"
    PaymentDto:
      type: "object"
      properties:
        methodId:
          type: "string"
        amount:
          "$ref": "#/components/schemas/DecimalDto"
        currencyCode:
          type: "string"
        processorsId:
          type: "string"
        transactions:
          type: "array"
          items:
            "$ref": "#/components/schemas/TransactionDto"
        issuedWalletKey:
          type: "string"
      required:
        - "methodId"
        - "amount"
        - "currencyCode"
        - "processorsId"
        - "transactions"
    TransactionInitDto:
      type: "object"
      properties:
        actions:
          type: "array"
          items:
            type: "string"
            enum:
              - "Authorize"
              - "Annul"
              - "Debit"
              - "Credit"
        token:
          type: "string"
        amount:
          "$ref": "#/components/schemas/DecimalDto"
        currencyCode:
          type: "string"
        methodId:
          type: "string"
        specification:
          "$ref": "#/components/schemas/SpecificationDto"
        walletCode:
          type: "string"
        isLocalTerminal:
          type: "boolean"
        terminalDirectUrl:
          type: "string"
        localProxy:
          "$ref": "#/components/schemas/LocalProxyDto"
        reversalArgs:
          "$ref": "#/components/schemas/ReversalDto"
      required:
        - "actions"
        - "token"
        - "amount"
        - "currencyCode"
        - "methodId"
    TransactionDto:
      type: "object"
      properties:
        actions:
          type: "array"
          items:
            type: "string"
            enum:
              - "Authorize"
              - "Annul"
              - "Debit"
              - "Credit"
        token:
          type: "string"
        amount:
          "$ref": "#/components/schemas/DecimalDto"
        currencyCode:
          type: "string"
        methodId:
          type: "string"
        specification:
          "$ref": "#/components/schemas/SpecificationDto"
        walletCode:
          type: "string"
        isLocalTerminal:
          type: "boolean"
        terminalDirectUrl:
          type: "string"
        localProxy:
          "$ref": "#/components/schemas/LocalProxyDto"
        reversalArgs:
          "$ref": "#/components/schemas/ReversalDto"
        transactionId:
          type: "string"
        timestamp:
          type: "string"
        means:
          "$ref": "#/components/schemas/PaymentMeansDto"
        consumerPrintout:
          type: "string"
        merchantPrintout:
          type: "string"
        rawData:
          type: "object"
      required:
        - "actions"
        - "token"
        - "amount"
        - "currencyCode"
        - "methodId"
        - "transactionId"
        - "timestamp"
    ReversalDto:
      type: "object"
      properties:
        originalTransactionId:
          type: "string"
        originalTimestamp:
          type: "string"
        terminalId:
          type: "string"
    CancelDto:
      type: "object"
      properties:
        terminalId:
          type: "string"
        terminalDirectUrl:
          type: "string"
        isLocalTerminal:
          type: "boolean"
        localProxy:
          "$ref": "#/components/schemas/LocalProxyDto"
    SpecificationDto:
      type: "array"
      items:
        "$ref": "#/components/schemas/SpecificationItemDto"
    SpecificationItemDto:
      type: "object"
      properties:
        identifier:
          type: "string"
        description:
          type: "string"
        quantity:
          "$ref": "#/components/schemas/DecimalDto"
        unit:
          type: "string"
        totalAmount:
          "$ref": "#/components/schemas/DecimalDto"
        vatPercentage:
          "$ref": "#/components/schemas/DecimalDto"
        currencyCode:
          type: "string"
      required:
        - "description"
        - "quantity"
        - "unit"
        - "totalAmount"
        - "currencyCode"
    PaymentMeansDto:
      oneOf:
        - "$ref": "#/components/schemas/PaymentCardDto"
        - "$ref": "#/components/schemas/SingletonPaymentMeansDto"
        - "$ref": "#/components/schemas/WalletPaymentMeansDto"
    PaymentCardDto:
      type: "object"
      properties:
        type:
          const: "Card"
        scheme:
          type: "string"
        issuer:
          type: "string"
        maskedPan:
          type: "string"
        token:
          type: "string"
        validFrom:
          type: "string"
        validTo:
          type: "string"
      required:
        - "type"
    SingletonPaymentMeansDto:
      type: "object"
      properties:
        type:
          const: "Singleton"
        id:
          type: "string"
      required:
        - "type"
        - "id"
    WalletPaymentMeansDto:
      type: "object"
      properties:
        type:
          const: "Wallet"
        walletId:
          type: "string"
        providerId:
          type: "string"
        provider:
          type: "string"
        maskedCode:
          type: "string"
        remainingBalance:
          type: "string"
        refillable:
          type: "boolean"
        validFrom:
          type: "string"
        validTo:
          type: "string"
      required:
        - "type"
    AgentDto:
      oneOf:
        - "$ref": "#/components/schemas/PersonDto"
        - "$ref": "#/components/schemas/OrganizationDto"
    PersonDto:
      type: "object"
      properties:
        key:
          type: "string"
        fullName:
          type: "string"
        nationalId:
          type: "string"
        email:
          type: "string"
        landlinePhone:
          type: "string"
        mobilePhone:
          type: "string"
        invoiceAddress:
          "$ref": "#/components/schemas/AddressDto"
        deliveryAddress:
          "$ref": "#/components/schemas/AddressDto"
        type:
          const: "Person"
        givenName:
          type: "string"
        familyName:
          type: "string"
        phoneNumber:
          type: "string"
      required:
        - "key"
        - "type"
    OrganizationDto:
      type: "object"
      properties:
        key:
          type: "string"
        fullName:
          type: "string"
        nationalId:
          type: "string"
        email:
          type: "string"
        landlinePhone:
          type: "string"
        mobilePhone:
          type: "string"
        invoiceAddress:
          "$ref": "#/components/schemas/AddressDto"
        deliveryAddress:
          "$ref": "#/components/schemas/AddressDto"
        type:
          const: "Organization"
      required:
        - "key"
        - "type"
    AddressDto:
      type: "object"
      properties:
        lines:
          type: "array"
          items:
            type: "string"
        postalCode:
          type: "string"
        cityName:
          type: "string"
        regionName:
          type: "string"
        countryCode:
          type: "string"
    EpiErrorDetail:
      type: "object"
      properties:
        code:
          type: "string"
        message:
          type: "string"
        params:
          type: "array"
          items:
            type: "string"
      required:
        - "message"
    ErrorBody:
      type: "object"
      properties:
        errors:
          type: "array"
          items:
            "$ref": "#/components/schemas/EpiErrorDetail"
      required:
        - "errors"
    StepCreate:
      type: "object"
      properties:
        type:
          const: "Create"
        result:
          "$ref": "#/components/schemas/PaymentDto"
      required:
        - "type"
        - "result"
    StepCancellable:
      type: "object"
      properties:
        type:
          const: "Cancellable"
        cancellationToken:
          type: "string"
      required:
        - "type"
        - "cancellationToken"
    StepWait:
      type: "object"
      properties:
        type:
          const: "Wait"
        message:
          type: "string"
        element:
          type: "string"
        translationKey:
          type: "string"
        params:
          type: "array"
          items:
            type: "string"
      required:
        - "type"
    StepShowImage:
      type: "object"
      properties:
        type:
          const: "ShowImage"
        url:
          type: "string"
        audience:
          "$ref": "#/components/schemas/Audience"
      required:
        - "type"
        - "url"
    StepVisitPage:
      type: "object"
      properties:
        type:
          const: "VisitPage"
        url:
          type: "string"
        audience:
          "$ref": "#/components/schemas/Audience"
      required:
        - "type"
        - "url"
    StepRenderView:
      type: "object"
      properties:
        type:
          const: "RenderView"
        path:
          type: "string"
        config:
          type: "string"
        audience:
          "$ref": "#/components/schemas/Audience"
      required:
        - "type"
        - "path"
        - "config"
    StepComplete:
      type: "object"
      properties:
        type:
          const: "Complete"
        result:
          "$ref": "#/components/schemas/PaymentDto"
        issuedWalletKey:
          type: "string"
      required:
        - "type"
        - "result"
    StepDecline:
      type: "object"
      properties:
        type:
          const: "Decline"
        reason:
          "$ref": "#/components/schemas/PaymentDeclineReason"
        params:
          type: "array"
          items:
            type: "string"
      required:
        - "type"
        - "reason"
    StepCancel:
      type: "object"
      properties:
        type:
          const: "Cancel"
      required:
        - "type"
    StepFail:
      type: "object"
      properties:
        type:
          const: "Fail"
        errors:
          type: "array"
          items:
            "$ref": "#/components/schemas/EpiErrorDetail"
      required:
        - "type"
        - "errors"
    TerminalPaymentStep:
      oneOf:
        - "$ref": "#/components/schemas/StepComplete"
        - "$ref": "#/components/schemas/StepDecline"
        - "$ref": "#/components/schemas/StepCancel"
        - "$ref": "#/components/schemas/StepFail"
    IntermediatePaymentStep:
      oneOf:
        - "$ref": "#/components/schemas/StepCreate"
        - "$ref": "#/components/schemas/StepCancellable"
        - "$ref": "#/components/schemas/StepWait"
        - "$ref": "#/components/schemas/StepShowImage"
        - "$ref": "#/components/schemas/StepVisitPage"
        - "$ref": "#/components/schemas/StepRenderView"
    PaymentStep:
      oneOf:
        - "$ref": "#/components/schemas/TerminalPaymentStep"
        - "$ref": "#/components/schemas/IntermediatePaymentStep"
