{
    "id": "H1",
    "title": "Headers: a contextful call without the context headers is rejected",
    "referenceOnly": true,
    "steps": [
        {
            "call": "methods",
            "stripContext": true,
            "expect": {
                "status": 400,
                "schema": "ErrorBody"
            }
        }
    ]
}
