{
    "id": "L1",
    "title": "Install: POST /install accepts the handshake payload",
    "steps": [
        {
            "call": "install",
            "args": "{{install}}",
            "expect": {
                "status": 200
            }
        }
    ]
}
